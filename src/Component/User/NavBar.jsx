import React, { useState } from "react";

function NavBar() {
  // const header = [[(id = 1), (data = "Parity")]];
  return (
    <div>
      <h3 style={{}} className="border-bottom text-center p-3">
        Parity
      </h3>
    </div>
  );
}

export default NavBar;
